# Copy to .env for the tests that spend money. .env is git-ignored; never commit a real key.
# Used by the live supervisor tests (see CONTRIBUTING.md) — the app itself stores your keys
# encrypted in the OS keychain, not here.
openrouter=sk-or-v1-...
